<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="center"
    android:background="@color/background"
    android:gravity="center">

    <TextView
        android:id="@+id/tv_message_setup_text_fragment"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:paddingBottom="50dp"
        android:paddingEnd="50dp"
        android:paddingStart="50dp"
        android:paddingTop="30dp"
        android:text="@string/first_use_welcome"
        android:textAlignment="center"
        android:textColor="@color/colorPrimary"
        android:textSize="16sp" />

    <ImageButton
        android:id="@+id/b_image_setup_text_fragment"
        android:layout_width="150dp"
        android:layout_height="180dp"
        android:layout_above="@+id/tv_message_setup_text_fragment"
        android:layout_centerHorizontal="true"
        android:layout_gravity="bottom"
        android:adjustViewBounds="true"
        android:background="@color/background"
        android:foregroundGravity="bottom"
        android:scaleType="fitCenter"
        android:src="@drawable/symbolsmall" />

</RelativeLayout>